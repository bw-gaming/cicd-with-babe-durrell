timeout: "600s"

# 1) Define which secrets to fetch from Secret Manager
secrets:
- secretEnv:
    GITHUB_TOKEN: "projects/nextwork-staging/secrets/GITHUB_TOKEN/versions/latest"
    SMTP_PASSWORD: "projects/nextwork-staging/secrets/SMTP_PASSWORD/versions/latest"

steps:
  # Step 1: Fetch the latest tag and create a GitHub release
  - name: "gcr.io/cloud-builders/curl"
    entrypoint: "bash"
    env:
      - GITHUB_TOKEN
    args:
      - "-c"
      - |
        REPO_OWNER="bw-gaming"
        REPO_NAME="bw-rbo"

        # Fetch the latest tag
        latest_tag=$(curl -s -H "Authorization: token $GITHUB_TOKEN" \
          "https://api.github.com/repos/$REPO_OWNER/$REPO_NAME/tags" | jq -r '.[0].name')

        if [ -z "$latest_tag" ]; then
          echo "Error: No tags found in the repository."
          exit 1
        fi

        echo "Latest tag: $latest_tag"

        # Create a release from the latest tag
        release_response=$(curl -s -X POST -H "Authorization: token $GITHUB_TOKEN" \
          -H "Content-Type: application/json" \
          -d '{
            "tag_name": "'"$latest_tag"'",
            "name": "Release '"$latest_tag"'",
            "body": "Automatically generated release for '"$latest_tag"'"
          }' "https://api.github.com/repos/$REPO_OWNER/$REPO_NAME/releases")

        release_name=$(echo "$release_response" | jq -r .name)
        release_body=$(echo "$release_response" | jq -r .body)

        if [ -z "$release_name" ] || [ -z "$release_body" ]; then
          echo "Error: Failed to create release. Response: $release_response"
          exit 1
        fi

        echo "## $release_name" > latest_release_note.txt
        echo "$release_body" >> latest_release_note.txt

  # Step 2: Prepare the email content
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: "bash"
    args:
      - "-c"
      - |
        date_str="$(date '+%Y-%m-%d %H:%M:%S')"
        release_note=$(cat latest_release_note.txt)

        cat <<EOF > release_email.txt
        Dear Team,

        A new production release is scheduled.

        **Details:**
        - **Start Time:** $date_str
        - **Release Tag:** $release_note

        **Release Notes:**
        $release_note

        Please find more details in the Teams channel: "Production Release".

        Regards,
        Automatic Notification System
        EOF

  # Step 3: Send the email via Outlook SMTP
  - name: "python:3.9"
    entrypoint: "python"
    env:
      - SMTP_PASSWORD
    args:
      - "-c"
      - |
        import smtplib
        from email.mime.text import MIMEText

        sender = "monitoring@bluewindowltd.com"
        receiver = "release-notifications@bluewindowltd.com"
        smtp_server = "smtp.office365.com"
        smtp_port = 587

        with open('release_email.txt', 'r', encoding='utf-8') as f:
            email_body = f.read()

        msg = MIMEText(email_body, "plain", "utf-8")
        msg["Subject"] = "Automatic Production Release Notification"
        msg["From"] = sender
        msg["To"] = receiver

        try:
            with smtplib.SMTP(smtp_server, smtp_port) as server:
                server.starttls()
                server.login(sender, "${SMTP_PASSWORD}")
                server.send_message(msg)
                print("Email sent successfully.")
        except Exception as e:
            print(f"Error sending email: {e}")
